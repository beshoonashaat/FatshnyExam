import { NextResponse } from 'next/server';import { isAdmin } from '@/lib/auth';import { getExamState,getStatusHistory } from '@/lib/storage';export const dynamic='force-dynamic';export async function GET(){if(!await isAdmin())return NextResponse.json({error:'UNAUTHORIZED'},{status:401});const[state,history]=await Promise.all([getExamState(true),getStatusHistory()]);return NextResponse.json({state,history},{headers:{'Cache-Control':'no-store'}})}
