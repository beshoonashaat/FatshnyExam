import { NextResponse } from 'next/server';import { isAdmin } from '@/lib/auth';import { setExamState } from '@/lib/storage';export const dynamic='force-dynamic';export async function POST(req:Request){if(!await isAdmin())return NextResponse.json({error:'UNAUTHORIZED'},{status:401});const b=await req.json();if(!['OPEN','CLOSED'].includes(b.status))return NextResponse.json({message:'Invalid status'},{status:400});const state=await setExamState(b.status,b.closeMode==='STOP_NEW_ATTEMPTS'?'STOP_NEW_ATTEMPTS':'STOP_ALL_SUBMISSIONS');return NextResponse.json({state},{headers:{'Cache-Control':'no-store'}})}
